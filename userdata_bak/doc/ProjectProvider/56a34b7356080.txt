如何在青云上安装：
1.下载 http://cncspace.newhua.com/down/xampp-linux-x64-5.6.12-0-installer.zip   /opt/
2 照这个步骤做；http://blog.csdn.net/keepaction/article/details/21279553
３.改端口：　http://bbs.csdn.net/topics/390084984
修改各个目录下的80端口为8034后，关键一步是
还要编辑根目录下的xampp文件，打开里面有句这样的话：
if testport 80
then


问：如何重启青云自带的nginx？
答：
1.修改
upstream enetf_web_servers {
    server 127.0.0.1:8034;
}
        location ~ ^/web/.*$ {
                proxy_connect_timeout 10;
                proxy_send_timeout 10;
                proxy_read_timeout 10;

                proxy_pass          http://enetf_web_servers;
                proxy_redirect      default;
                proxy_set_header    X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header    X-Real-IP $remote_addr;
                proxy_set_header    Host $http_host;
                proxy_set_header    Range $http_range;

                proxy_next_upstream http_502 http_504 error timeout invalid_header;
        }
2.然后重启自带的nginx   sudo /usr/sbin/nginx -s reload

注意：不要覆盖之前的/opt/lampp !!!


